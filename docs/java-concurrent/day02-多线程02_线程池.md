## day02线程池

### 1. 为什么使用线程池

- 避免频繁创建/销毁线程,节省资源
- 限制最大线程数 防止系统崩溃
- 复用线程,提升吞吐量
- 支持定时任务,延迟执行等高级功能
- 提供任务队列  拒绝策略 

### 2. 核心线程池类图结构

> 核心线程池类是:
>
> ThreadPoolExecutor 实现了Executor 和 ExecutorService接口
>
> 核心构造器:
>
> ```java
> public ThreadPoolExecutor(
>     int corePoolSize,              // 核心线程数
>     int maximumPoolSize,           // 最大线程数
>     long keepAliveTime,            // 非核心线程最大空闲时间
>     TimeUnit unit,                 // 时间单位
>     BlockingQueue<Runnable> workQueue, // 任务队列
>     ThreadFactory threadFactory,   // 线程工厂（可定制线程名）
>     RejectedExecutionHandler handler // 拒绝策略
> )
> ```
>
> 源码类图结构
>
> Executor  
>
> ​	└── ExecutorService        
>
> ​		└── AbstractExecutorService              
>
> ​			 	└── ThreadPoolExecutor

### 3. 工作流程图解

> 当你调用`execute(task)`时，线程池执行流程如下：
>
> 1.  当前运行线程数 < corePoolSize → 创建核心线程执行任务 
> 2.  否则任务入队到`BlockingQueue` 
> 3.  若队列满，且线程数 < maxPoolSize → 创建非核心线程执行任务 
> 4.  否则 → 触发拒绝策略！
>
> ```
> task --->  		|   线程池    |
>                 +-----+------+
>                       |
>         ------------------------------
>         | core 线程未满？ → 创建新线程
>         | 队列未满？     → 入队排队
>         | max线程未满？ → 创建非核心线程
>         | 否则          → 拒绝策略
> ```

### 4. 实战案例

```java
//提交20个耗时任务,线程池批量并发处理
import java.util.concurrent.*;

public class ThreadPoolDemo {
    public static void main(String[] args) throws InterruptedException {
        ExecutorService pool = new ThreadPoolExecutor(
            4, 8, 30L, TimeUnit.SECONDS,
            new ArrayBlockingQueue<>(10),
            Executors.defaultThreadFactory(),
            new ThreadPoolExecutor.CallerRunsPolicy()
        );

        for (int i = 1; i <= 20; i++) {
            final int taskId = i;
            pool.execute(() -> {
                System.out.println("任务 " + taskId + " 执行中，线程：" + Thread.currentThread().getName());
                try {
                    Thread.sleep(1000); // 模拟任务耗时
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            });
        }

        pool.shutdown();
        pool.awaitTermination(1, TimeUnit.MINUTES);
        System.out.println("所有任务执行完毕");
    }
}
```

> 执行结果:
>
> 任务 3 执行中，线程：pool-1-thread-3
> 任务 4 执行中，线程：pool-1-thread-4
> 任务 18 执行中，线程：pool-1-thread-8
> 任务 15 执行中，线程：pool-1-thread-5
> 任务 19 执行中，线程：main
> 任务 16 执行中，线程：pool-1-thread-6
> 任务 17 执行中，线程：pool-1-thread-7
> 任务 1 执行中，线程：pool-1-thread-1
> 任务 2 执行中，线程：pool-1-thread-2
> 任务 6 执行中，线程：pool-1-thread-2
> 任务 5 执行中，线程：pool-1-thread-6
> 任务 8 执行中，线程：pool-1-thread-8
> 任务 7 执行中，线程：pool-1-thread-7
> 任务 9 执行中，线程：pool-1-thread-4
> 任务 12 执行中，线程：pool-1-thread-5
> 任务 10 执行中，线程：pool-1-thread-3
> 任务 11 执行中，线程：pool-1-thread-1
> 任务 13 执行中，线程：pool-1-thread-6
> 任务 20 执行中，线程：pool-1-thread-2
> 任务 14 执行中，线程：pool-1-thread-7
> 所有任务执行完毕

### 5. 内置线程池工具类 Executors简介

Java提供了常用线程池工厂方法

| 方法                                | 特点                     |
| :---------------------------------- | :----------------------- |
| Executors.newFixedThreadPool(n)     | 固定大小线程池           |
| Executors.newCachedThreadPool()     | 自动扩容（线程数无上限） |
| Executors.newSingleThreadExecutor() | 单线程串行执行           |
| Executors.newScheduledThreadPool(n) | 定时任务线程池           |

推荐使用自定义ThreadPoolExecutor 而非Executors工具类,因为默认参数可能导致OOM或线程泄露

### 6. 拒绝策略 重点

当任务队列已满且线程数达到最大值，会触发拒绝策略：

| 策略                | 行为                         |
| :------------------ | :--------------------------- |
| AbortPolicy（默认） | 抛出异常                     |
| CallerRunsPolicy    | 用调用者线程执行任务         |
| DiscardPolicy       | 静默丢弃任务                 |
| DiscardOldestPolicy | 丢弃最早排队任务，再尝试提交 |

推荐使用CallerRunsPolicy:任务不会丢失,但会回退到调用线程执行,保证线程不雪崩

### 7. 线程池最佳实践建议

- 设置合理线程数 一般为CPU核心数+1 (IO密集型可再高)
- 尽量使用有界队列:防止任务无限堆积导致OOM
- 自定义线程工厂 方便排查问题 如设置线程名
- 拒绝策略要合理选择 不能静默丢任务
- 任务尽量短小精悍 避免长期占用线程

### 8. 生产环境常用场景

- 秒杀系统中异步落库
- 短信/邮件发送任务
- 导出Excel,PDF后台生成
- 大批量数据分页并发处理
- 定时调度器任务线程池

### 9. 线程池监控指标

以下方式可以获取线程池运行状态

```Java
ThreadPoolExecutor executor = (ThreadPoolExecutor) pool;
System.out.println("核心线程数: " + executor.getCorePoolSize());
System.out.println("活跃线程数: " + executor.getActiveCount());
System.out.println("任务完成数: " + executor.getCompletedTaskCount());
```

